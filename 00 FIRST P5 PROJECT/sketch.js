// Project Title
// Your Name
// Date
// One line description


function setup() {
  // happens exactly once, at the start of our program
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  // happens over and over again (60 fps)
  // background(220);
  circle(mouseX, mouseY, 30);
}
