let obama;

function setup() {
	new Canvas(60, 10);
	  //         (    mode, renderQuality, scale)
	  displayMode('centered', 'pixelated');

	background('white');
	noStroke();
}

function draw() {

}
